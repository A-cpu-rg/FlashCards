<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Newton Flash Cards - Landing Page</title>
  <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
  
  <style>
    body {
      font-family: 'Poppins', sans-serif;
      background-color: #f5f5f5;
      color: #333;
      margin: 0;
    }

    /* Navbar Styling */
    nav {
      background-color: #007bff;
      padding: 15px;
      box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
    }
    nav a {
      color: white;
      font-size: 1.2rem;
      margin: 0 15px;
    }

    /* Main Section Styling */
    .welcome-section {
      background-color: #fff;
      padding: 50px;
      box-shadow: 0 4px 20px rgba(0, 0, 0, 0.1);
      border-radius: 10px;
      text-align: center;
      max-width: 600px;
      margin: auto;
      transition: transform 0.3s ease;
    }

    /* Animation on hover */
    .welcome-section:hover {
      transform: scale(1.05);
    }

    h1 {
      font-size: 2.8rem;
      color: #007bff;
      font-weight: 700;
    }

    p {
      font-size: 1.2rem;
      margin-top: 15px;
      color: #555;
    }

    .btn-primary {
      margin-top: 30px;
      padding: 10px 20px;
      font-size: 1.1rem;
      border-radius: 50px;
      background-color: #28a745;
      border: none;
      transition: background-color 0.3s ease;
    }

    .btn-primary:hover {
      background-color: #218838;
    }

    /* Footer Styling */
    footer {
      position: fixed;
      bottom: 0;
      width: 100%;
      text-align: center;
      padding: 10px;
      background-color: #007bff;
      color: white;
    }
  </style>
</head>
<body>
  <!-- Navbar -->
  <nav class="d-flex justify-content-center">
    <a href="/">Home</a>
    <a href="/user/signin">signin</a>
    <a href="/user/signup">create new account</a>
  </nav>

  <!-- Main Content Section -->
  <div class="container d-flex justify-content-center align-items-center" style="height: calc(100vh - 100px);">
    <div class="welcome-section">
      <h1>Welcome to Newton Flash Cards!</h1>
      <p>Master your concepts through dynamic flash cards and interactive learning.</p>
      <a href="/user/signup" class="btn btn-primary">Get Started</a>
    </div>
  </div>

  <!-- Footer -->
  <footer>
    &copy; 2024 Newton Flash Cards | Empowering Learning
  </footer>

  <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.5.3/dist/umd/popper.min.js"></script>
  <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>
</html>
